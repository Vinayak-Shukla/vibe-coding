---
description: Set playback volume (0-100)
allowed-tools: Bash
argument-hint: "<0-100>"
---

Set the volume. Run:

```
node "${CLAUDE_PLUGIN_ROOT}/bin/client.js" volume $ARGUMENTS
```

Confirm the new volume level to the user.
