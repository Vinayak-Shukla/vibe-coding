---
description: Toggle auto-play on/off — when on, music plays automatically while Claude thinks
allowed-tools: Bash
---

Toggle auto-play. Run:

```
node "${CLAUDE_PLUGIN_ROOT}/bin/client.js" toggle
```

Tell the user the new auto-play state.
