---
description: Show current vibe-coding status — what's playing, volume, auto-play state, available vibes
allowed-tools: Bash
---

Show the current status. Run:

```
node "${CLAUDE_PLUGIN_ROOT}/bin/client.js" status
```

Display the output to the user in a readable format.
